language: python
python:
  - "3.8"
install:
  - pip install -r requirements.txt
  - pip install .[dev]
script:
  - pipdeptree
  - coverage run --source=s3seek -m pytest
  - coverage report -m
  #- mypy s3seek
